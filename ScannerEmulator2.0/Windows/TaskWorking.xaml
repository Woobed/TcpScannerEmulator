<UserControl x:Class="ScannerEmulator2._0.Windows.TaskWorking"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Viewbox Stretch="Uniform" StretchDirection="Both">
        <Border Width="980" Height="630" Background="White" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- === Заголовок === -->
                <TextBlock Grid.Row="0" Text="🎥 Управление активными камерами"
                           FontSize="26" FontWeight="Bold"
                           Margin="10,0,0,20"/>

                <!-- === Список камер === -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="ActiveCamerasPanel">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#DDD" BorderThickness="1" Background="WhiteSmoke"
                                        CornerRadius="8" Padding="12" Margin="6">
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal">
                                            <StackPanel Orientation="Vertical">
                                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14"/>
                                                <TextBlock Text="{Binding FileName}" 
                                                            FontSize="12"
                                                            Foreground="Gray"
                                                            TextTrimming="CharacterEllipsis"
                                                            Margin="0,4,0,0"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Vertical" Margin="40,0,0,0">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Grid.Row="0" Text="Header" Width="70" TextAlignment="Center" FontSize="14"  Margin="8,0,0,3"/>
                                                    <TextBlock Grid.Row="0" Text="Terminator" Width="70" TextAlignment="Center" FontSize="14"  Margin="8,0,0,3"/>
                                                    <TextBlock Grid.Row="0" Text="Separator" Width="70" TextAlignment="Center" FontSize="14" Margin="7,0,0,3"/>
                                                    <TextBlock Grid.Row="0" Text="Delay" Width="70" TextAlignment="Center" FontSize="14" Margin="6.5,0,0,3"/>
                                                    <TextBlock Grid.Row="0" Text="GroupCount" Width="70" TextAlignment="Center" FontSize="14" Margin="6.5,0,0,3"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal">
                                                    
                                                    <TextBox Text="{Binding DataHeader, UpdateSourceTrigger=PropertyChanged}" 
                                                            Height="25" Width="70" Margin="8,0,0,8" FontSize="14" 
                                                            ToolTip="Заголовок данных"/>
                                                    <TextBox Text="{Binding DataTerminator, UpdateSourceTrigger=PropertyChanged}" 
                                                            Height="25" Width="70" Margin="8,0,0,8" FontSize="14" 
                                                            ToolTip="Терминатор данных"/>
                                                    <TextBox Text="{Binding DataSeparator, UpdateSourceTrigger=PropertyChanged}" 
                                                            Height="25" Width="70" Margin="8,0,0,8" FontSize="14" 
                                                            ToolTip="Разделитель данных"/>
                                                    <TextBox Text="{Binding Delay, UpdateSourceTrigger=PropertyChanged}" 
                                                            Height="25" Width="70" Margin="8,0,0,8" FontSize="14" 
                                                            ToolTip="Задержка в миллисекундах"/>
                                                    <TextBox Text="{Binding GroupCount, UpdateSourceTrigger=PropertyChanged}" 
                                                            Height="25" Width="70" Margin="8,0,0,8" FontSize="14" 
                                                            ToolTip="Количество в группе"/>
                                                </StackPanel>
                                             </StackPanel>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Button Content="▶ Начать"
                                                    Tag="{Binding Name}"
                                                    Click="Start_Click"
                                                    Margin="4"
                                                    Padding="12,4"
                                                    FontSize="13"/>
                                            <Button Content="⏸ Пауза"
                                                    Tag="{Binding Name}"
                                                    Click="Pause_Click"
                                                    Margin="4"
                                                    Padding="12,4"
                                                    FontSize="13"/>
                                            <Button Content="⏵ Продолжить"
                                                    Tag="{Binding Name}"
                                                    Click="Resume_Click"
                                                    Margin="4"
                                                    Padding="12,4"
                                                    FontSize="13"/>
                                            <Button Content="⏹ Прервать"
                                                    Tag="{Binding Name}"
                                                    Click="Stop_Click"
                                                    Margin="4"
                                                    Padding="12,4"
                                                    FontSize="13"
                                                    Background="#ffdddd"/>
                                        </StackPanel>

                                        <TextBlock Text="{Binding Status}"
                                                   FontStyle="Italic"
                                                   Foreground="Gray"
                                                   FontSize="12"
                                                   HorizontalAlignment="Center"
                                                   Margin="0,10,0,0"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- === Нижняя панель === -->
                <TextBlock Grid.Row="2" x:Name="StatusLabel"
                           Text="Отображаются все активные камеры"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontStyle="Italic"
                           Foreground="Gray"
                           Margin="0,15,0,0"/>
            </Grid>
        </Border>
    </Viewbox>
</UserControl>
