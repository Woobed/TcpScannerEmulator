<UserControl x:Class="ScannerEmulator2._0.Windows.TaskCreating"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Height="650" Width="1000">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <!-- === Раздел 1: Файлы === -->
        <Border Grid.Column="0" Margin="10" Background="WhiteSmoke" Padding="10"
                BorderBrush="#DDD" BorderThickness="1" CornerRadius="6">
            <StackPanel>
                <TextBlock Text="📂 Файлы" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                <ListBox x:Name="FilesListBox" Height="400"/>
                <Button Content="Выбрать файл" Margin="0,10,0,0" Height="35" Click="SelectFile_Click"/>
                <TextBlock x:Name="SelectedFileLabel" Text="Файл не выбран"
                           Margin="0,10,0,0" FontStyle="Italic" Foreground="Gray"/>
            </StackPanel>
        </Border>

        <!-- === Раздел 2: Просмотр файла === -->
        <Border Grid.Column="1" Margin="10" Background="WhiteSmoke" Padding="10"
                BorderBrush="#DDD" BorderThickness="1" CornerRadius="6">
            <StackPanel>
                <TextBlock Text="📄 Содержимое файла" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                <TextBox x:Name="FileContentTextBox"
                         Text="Выберите файл, чтобы просмотреть содержимое..."
                         IsReadOnly="True" AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         Height="500"
                         Background="White"/>
            </StackPanel>
        </Border>

        <!-- === Раздел 3: Камеры === -->
        <Border Grid.Column="2" Margin="10" Background="WhiteSmoke" Padding="10"
                BorderBrush="#DDD" BorderThickness="1" CornerRadius="6">
            <StackPanel>
                <TextBlock Text="📸 Камеры" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

                <StackPanel Orientation="Horizontal" Margin="0,5,5,20" HorizontalAlignment="Center" >
                    <TextBlock Text="IP:" VerticalAlignment="Center" Width="40"/>
                    <TextBox x:Name="IpTextBox" Width="120" Text="127.0.0.1"/>
                    <TextBlock Text="Port:" VerticalAlignment="Center" Width="50" Margin="10,0,0,0"/>
                    <TextBox x:Name="PortTextBox" Width="80" Text="9000"/>
                </StackPanel>
                <Button Content="Создать" Margin="10,0,0,0" Width="100" Click="CreateCamera_Click"/>
                <TextBlock Text="Список камер:" Margin="0,10,0,5" FontWeight="Bold"/>
                
                <ListBox x:Name="CamerasListBox" Height="400">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#CCC" BorderThickness="1" Padding="5" Margin="3" CornerRadius="4" Background="White">
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                        <Button Content="Назначить файл"
                                            Tag="{Binding Name}"
                                            Click="AssignFile_Click"
                                            Margin="0,0,5,0"/>
                                        <Button Content="Старт"
                                            Tag="{Binding Name}"
                                            Click="StartCamera_Click"
                                            Margin="0,0,5,0"/>
                                        <Button Content="Удалить"
                                            Tag="{Binding Name}"
                                            Click="DeleteCamera_Click"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                
            </StackPanel>
        </Border>
    </Grid>
</UserControl>